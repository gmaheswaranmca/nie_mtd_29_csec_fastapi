Recap: 
    1. what is Fast API?
        ASGI based python web framework 
            - Starlette - Pydantic - Documentation

            - Documentation : /docs /redoc openapi spec json
    2. How to run the Fast API?
        uvicorn main:app --reload 

    3. How to test Fast API? 
        using Swagger 
        Thunder Client or Postman 

    4. What did we learn so far?
        mongo db basic operations CRUD (create, read, update, delete)
        basic fastapi app 
        crud operations on in-mem dict db 
Agenda
        crud operations on mongo 
            to run: uvicorn main:app --reload
            browser: http://127.0.0.1:8000/docs 

            1. create 3 tickets
                "laptop is not on"
                    check is it created
                "web cam is not working"
                    check is it created
                "os - file system not working"
                    check is it created
            2. read first ticket by id 
            3. update first ticket status as "ASSIGNED"
                check it is updated using "get all"
            4. delete third ticket 
                check it is deleted using "get all" 


            in mongosh:
            test> show dbs
            test> use service_ticket_db
            service_ticket_db> show collections
            service_ticket_db> db.tickets.find()

        secured app
            authentication (authN) who is the user
            authorization (authZ) what is role / privileges of the user 

            jwt (js web token) - token 

            tool : draw.io -> https://app.diagrams.net/

            token bases resources / APIs 
                - create (1,2,3), read all/search (1,2,3,4), 
                   read by id (1,2,3,4), update(3,2,1), 
                   delete (4) 
                   
                - Admin 4 | TL 3 | SE 2 | Employee 1

            token less resources / APIs 
                - login 
                - user creation 

            pip install pyjwt pwdlib
            pip install "pwdlib[argon2]"
            pip install python-multipart

            HOF - higher order function 
            what it is? 
            the fn that takes fn as args and / or returns a fn

            require_roles HOF 
                which has return value which is check_role
                check_role has arg current_user whose value is 
                supplied by FastAPI using DI "get_current_user"

                "get_current_user" takes token as arg 
                which supplied by FastAPI using DI oauth2_scheme

            APIs 
                user 
                    POST /users 
                    POST /login 
                ticket  
                    POST /tickets 
                    GET /tickets 
                    GET /tickets/{id}
                    PUT /tickets/{id}
                    DELETE /tickets/{id}
        - backend app (server) business logics 
        - frontend app (client) ui logics

        - docker compose to run three apps - orchestration


